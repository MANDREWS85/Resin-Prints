import Button from "./Button"

export default function NavBar() {
  const id="NavBar"
  return(
    <div id={id}>
      
      <Button 
        text="BUY Monstera Deliciosa"  
      />

      <Button 
        text="LINK"  
      />

      <Button 
        text="LINK"  
      />

      <Button 
        text="LINK"  
      />
      
    </div>
  )

  
}